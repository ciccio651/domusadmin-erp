# Cloudflare Pages — Configurazione wrangler
# Nome progetto: domusadmin-erp
# Build: npm run build -> dist/
# SPA routing: _redirects gestisce le route React

name = "domusadmin-erp"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
